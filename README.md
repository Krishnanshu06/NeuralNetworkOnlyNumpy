# NeuralNetworkOnlyNumpy
This is a neural network written in python using just NumPy and python . Then trained on the MNIST digit dataset achieving very good accuracy.

/n
The test.y file was generated by ChatGpt the rest was entirely me.


TO RUN:
- Run the test.py file to use the trained model to predict a digit drawn.

TO TRAIN:
- The the trainer and tune hyperparameters to use the model as you want.
